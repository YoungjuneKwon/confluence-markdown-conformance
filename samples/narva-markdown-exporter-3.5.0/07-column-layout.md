# 07 Column layout

Column layouts are a pure presentation construct. Markdown has no columns, so the only correct behaviour is to flatten in reading order without losing text.

<a id="c71-left-column"></a>

## C7.1 Left column

Text that lives in the left column.

- left one
- left two

<a id="c72-right-column"></a>

## C7.2 Right column

Text that lives in the right column.

```
print("inside a layout cell")
```

C7.3 First of three.

C7.4 Second of three.

C7.5 Third of three.